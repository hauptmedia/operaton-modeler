name: TEST
permissions:
  contents: read
on:
  push:
    branches:
      - main
      - develop
  pull_request:
  workflow_dispatch:
jobs:
  build:
    strategy:
      matrix:
        os: [ ubuntu-latest, macos-latest, windows-2022  ]
    runs-on: ${{ matrix.os }}
    env:
      NODE_TLS_REJECT_UNAUTHORIZED: 0
      IS_CI: true
      BUILD_REF: ${{ github.event.pull_request.number || github.ref_name }}
      BUILD_NUMBER: ${{ github.run_number }}-${{ github.run_id }}
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 22
        cache: 'npm'
    - name: Install npm dependencies
      run: npm install
    - name: Test Modeler
      run: |
        npm install puppeteer
        npm run test

    - name: Get version (sets APP_VERSION)
      id: get-version
      run: bash ./scripts/get_version_for_artifact_name.sh

    - name: Build distro (Linux)
      if: ${{ runner.OS == 'Linux' }}
      env:
        ELECTRON_GET_USE_PROXY: true
      run: npm run build -- --linux

    - name: Build distro (MacOS)
      if: ${{ runner.os == 'macOS' }}
      env:
        ELECTRON_GET_USE_PROXY: true
      run: npm run build -- --mac

    - name: Build distro (Windows)
      if: ${{ runner.os == 'Windows' }}
      env:
        ELECTRON_GET_USE_PROXY: true
      run: npm run build -- --win

    - name: Upload build artifacts ${{ matrix.os }}
      uses: actions/upload-artifact@v4
      with:
        name: operaton-modeler-${{ steps.get-version.outputs.APP_VERSION }}-${{ matrix.os }}
        path: dist
